# Git files
.git
.gitignore

# Node modules (these will be installed within the Docker image)
node_modules

# Next.js build output (this will be generated within the Docker image)
.next

# VSCode specific files
.vscode

# Development specific files
README.md
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment files (should be passed at runtime, not built into image)
.env
.env*.local
